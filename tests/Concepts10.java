/*
In the statement "int x" which word is a keyword and which word is a user-defined identifier? What is the difference between the two?

x is the identifier
y is a keyword.

Keywords are reserved as special for the entire programming language. Identifiers are reserved by the programmer and are specific to just the current program or current scope insde the program where they are defined.
*/
